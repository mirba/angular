export declare enum LOG_LEVEL {
    info = "info",
    warning = "warning",
    error = "error",
}
export declare class TraceService {
    private config;
    private logLevel;
    constructor(config: any);
    info(message: string): void;
    warning(message: string): void;
    error(message: string): void;
    private log(message, severity);
}
