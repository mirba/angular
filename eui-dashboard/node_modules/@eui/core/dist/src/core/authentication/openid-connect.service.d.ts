import { HttpClient } from '@angular/common/http';
export declare class OpenidConnectService {
    private config;
    protected http: HttpClient;
    SESSION_STORAGE_KEY_ID_TOKEN: string;
    SESSION_STORAGE_KEY_USER_DETAILS: string;
    REQUEST_PARAMETER_ORIGINAL_URL: string;
    DEFAULT_OPENID_SCOPE: string;
    constructor(config: any, http: HttpClient);
    getIdToken(): string;
    getUserDetails(): any;
    renewIdToken(): void;
    protected decodeJwtToken(jwtToken: any): any;
}
