import { UxPermissionService } from './ux-permission.service';
import { StoreService } from '../state/index';
import { UxLink } from '../models';
export declare class UxDynamicMenuService {
    protected config: any;
    private uxPermission;
    private store;
    menuLinks: Array<any>;
    constructor(config: any, uxPermission: UxPermissionService, store: StoreService);
    getMenuLinks(): any[];
    filterUxLinksWithRights(links: Array<UxLink>): Array<UxLink>;
}
