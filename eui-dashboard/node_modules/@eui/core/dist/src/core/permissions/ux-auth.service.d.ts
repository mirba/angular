export declare class UxAuthService {
    protected config: any;
    isLoggedIn: boolean;
    redirectUrl: string;
    loginPageUrl: string;
    constructor(config: any);
    setLoggedIn(status: boolean): void;
    setRedirectUrl(url: string): void;
}
