import { Store } from '@ngrx/store';
import { Rights } from './ux-permissions.model';
export declare class UxPermissionService {
    private store;
    userRights: Array<Rights>;
    constructor(store: Store<any>);
    getRights(): Array<Rights>;
    checkRightToActivateRoute(routeId: any): boolean;
    checkAttributePermission(permStr: string): boolean;
    private checkRight(rights, rightId);
    private checkPermission(rights, rightId, permissionId);
}
