import { Observable } from 'rxjs/Observable';
import { HttpService } from '../http/http.service';
import { User } from './user.model';
import { StoreService } from '../state/index';
export declare class UserService {
    private config;
    private store;
    private http;
    constructor(config: any, store: StoreService, http: HttpService);
    loadUserDetail(): Observable<User>;
    observePreferencesUpdate(): void;
    loadUserPreferences(): Observable<any>;
    saveUserPreferences(prefs: any): Observable<any>;
    userPreferences(): Observable<any>;
    private getUserId();
    private getCoreApiConfig();
    private getEndPoint(target?);
}
