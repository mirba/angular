import { HttpClient } from '@angular/common/http';
import { EventEmitter } from '@angular/core';
import { TranslateService } from '@ngx-translate/core';
import { Observable } from 'rxjs/Observable';
export declare class HttpService {
    private http;
    private translateService;
    /**
     * The request initialized event emitter
     */
    requestInit: EventEmitter<{}>;
    /**
     * The request resolved event emitter
     */
    requestResolved: EventEmitter<{}>;
    /**
     * The request failed event emitter
     */
    requestFailed: EventEmitter<{}>;
    constructor(http: HttpClient, translateService: TranslateService);
    get(uri: string, options?: any): Observable<any>;
    post(uri: string, body: any, options?: any): Observable<any>;
    put(uri: string, body: any, options?: any): Observable<any>;
    delete(uri: string, options?: any): Observable<any>;
    private emitRequestInitEvent(verb, uri, options);
    private emitRequestResolvedEvent(verb, uri, options, response);
    private emitRequestFailedEvent(verb, uri, options);
    private addLangToUri(uri);
}
