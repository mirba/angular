import { HttpInterceptor, HttpEvent, HttpRequest, HttpHandler } from '@angular/common/http';
import { Router } from '@angular/router';
import { Observable } from 'rxjs/Observable';
import { TraceService } from '../../trace/trace.service';
import { UxService } from '../../app/ux.service';
export declare enum ResponseErrorHandlerTypes {
    Redirect = 1,
    Notify = 2,
    None = 3,
}
export declare class RequestErrorInterceptor implements HttpInterceptor {
    private appConfig;
    private logger;
    private router;
    private uxService;
    constructor(appConfig: any, logger: TraceService, router: Router, uxService: UxService);
    intercept(req: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>>;
    private showNotificationError(err);
    private logError(err);
}
