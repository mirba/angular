import { InjectionToken } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { TranslateLoader } from '@ngx-translate/core';
export declare class TranslateHttpLoader implements TranslateLoader {
    private http;
    private prefix;
    private suffix;
    private queryParams;
    constructor(http: HttpClient, prefix?: string, suffix?: string, queryParams?: boolean | string);
    /**
     * Gets the translations from the server
     * @param lang
     * @returns any
     */
    getTranslation(lang: string): any;
}
export declare const translateConfig: {
    loader: {
        provide: typeof TranslateLoader;
        useFactory: (http: HttpClient, config: any) => TranslateHttpLoader;
        deps: (InjectionToken<{}> | typeof HttpClient)[];
    };
};
export declare function translateStaticLoader(http: HttpClient, config: any): TranslateHttpLoader;
