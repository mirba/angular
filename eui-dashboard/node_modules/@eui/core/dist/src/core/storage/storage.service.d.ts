import { LocalstorageService } from './localstorage.service';
import { TraceService } from '../trace/trace.service';
export declare class StorageService {
    private localStorageService;
    private logger;
    private _storage;
    constructor(localStorageService: LocalstorageService, logger: TraceService);
    set(key: string, data: any, storage?: string): any;
    get(key: string, storage?: string): any;
    delete(key: string, storage?: string): void;
    storage: string;
}
