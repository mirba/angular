import { Observable } from 'rxjs/Observable';
import { HttpService } from '../http/http.service';
export declare class EnvConfigService {
    private http;
    private config;
    constructor(http: HttpService, config: any);
    loadEnvConfigFile(): Observable<any>;
    loadEnvConfigService(fileConfig: any): Observable<any>;
    loadDynamicConfig(): Observable<any>;
}
