import { Actions } from '@ngrx/effects';
import { Observable } from 'rxjs/Observable';
import { StoreService } from '../store.service';
import { UxService } from '../../app/ux.service';
export declare class AppEffects {
    private actions;
    private store;
    private uxService;
    connectionUpdate: Observable<{
        type: string;
        payload: boolean;
    }>;
    connectionUpdateConfirmed: Observable<any>;
    constructor(actions: Actions, store: StoreService, uxService: UxService);
}
