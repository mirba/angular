export interface AppState {
    currentModule: string;
    version: string;
    connected: boolean;
    status: string;
    apiQueue: any;
}
export declare const initialState: AppState;
export declare const reducer: (state: AppState, action: any) => AppState;
