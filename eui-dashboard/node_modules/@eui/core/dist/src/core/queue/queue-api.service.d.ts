import { Observable } from 'rxjs/Observable';
import { HttpService } from '../http/http.service';
import { StoreService } from '../state/index';
import { TraceService } from '../trace/trace.service';
export declare class QueueApiService {
    private store;
    private http;
    private logger;
    constructor(store: StoreService, http: HttpService, logger: TraceService);
    queue(queueId: string, uri: string, method: string, payload?: any): void;
    removeQueue(id: string): void;
    processQueue(queueId: string): Observable<{}>;
}
