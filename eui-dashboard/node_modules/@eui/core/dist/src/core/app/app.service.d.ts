import { StoreService } from '../state/store.service';
export declare class AppService {
    private store;
    constructor(store: StoreService);
    /**
     * Start listening for navigator disconnection
     * and updates the store
     */
    listenForConnectionUpdate(): void;
}
