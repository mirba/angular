import { Observable } from 'rxjs/Observable';
import { UxService } from '../app/ux.service';
import { HttpService } from '../http/http.service';
import 'rxjs/add/operator/map';
export declare class UxAssetsService {
    protected http: HttpService;
    protected uxService: UxService;
    constructor(http: HttpService, uxService: UxService);
    getAssetsContents(assetId: string, languageCode: string, remoteUrl?: string): Observable<any>;
}
