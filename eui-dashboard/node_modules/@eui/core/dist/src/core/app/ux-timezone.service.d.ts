export declare class UxTimezoneService {
    constructor();
    /**
     * Convert country ISO code to country name (in english)
     */
    iso2country(iso: string): string;
    /**
     * Gets the list of ISO-codes for all countries
     */
    getCountries(): string[];
    getTimezones(): any[];
    getTimezone(tz: string): any;
}
