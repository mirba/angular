import { PlacementOptions } from './placement-options';
import { PopoverVM } from './../viewmodel/ux-dynamic-popover.viewmodel';
export interface PlacementStrategy {
    getId(): string;
    calculate(placementOptions: PlacementOptions): PopoverVM;
}
