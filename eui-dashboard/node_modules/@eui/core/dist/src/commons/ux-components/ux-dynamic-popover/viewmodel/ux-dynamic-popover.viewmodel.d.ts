import { Position } from './../../../shared/geometry/interface/position';
import { Offset } from './../../../shared/geometry/model/offset';
export interface PopoverDetails {
    placementClassModifier: string;
    popoverPosition: Position;
    arrowOffset: Offset;
}
export declare class PopoverVM implements PopoverDetails {
    placementClassModifier: string;
    popoverPosition: Position;
    arrowOffset: Offset;
    private constructor();
    static create(popoverDetails?: PopoverDetails): PopoverVM;
}
