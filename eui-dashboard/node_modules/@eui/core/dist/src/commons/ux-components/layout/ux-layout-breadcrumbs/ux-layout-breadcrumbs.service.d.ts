import { ActivatedRoute } from '@angular/router';
import { TranslateService } from '@ngx-translate/core';
export declare class UxBreadcrumbsService {
    private translateService;
    private labels;
    constructor(translateService: TranslateService);
    buildUrl(route: ActivatedRoute): string;
    setLabel(route: ActivatedRoute, label: string): void;
    getLabel(route: ActivatedRoute): string;
}
