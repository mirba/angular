import { QueryList, AfterContentInit, EventEmitter, OnChanges, SimpleChanges, ElementRef } from '@angular/core';
import { UxService } from '../../../core/app/ux.service';
import { UxWizardStepComponent } from './ux-wizard-step.component';
import { UxWizardStep } from './ux-wizard-step';
export declare class UxWizardStepsComponent implements AfterContentInit, OnChanges {
    protected uxService: UxService;
    isCustomContent: boolean;
    isShowStepTitle: boolean;
    isNavigationAllowed: boolean;
    activeStepIndex: number;
    steps: Array<UxWizardStep>;
    tabindex: number;
    selectStep: EventEmitter<any>;
    childrenSteps: QueryList<UxWizardStepComponent>;
    focusables: QueryList<ElementRef>;
    stepContentId: string;
    stepIds: string;
    constructor(uxService: UxService);
    ngAfterContentInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    onSelectStep(step: UxWizardStep, index: number): void;
    onKeyDown(event: any): void;
    protected selectPreviousStep(): void;
    protected selectNextStep(): void;
    private _selectStep(step, index);
    private _getStep(index);
}
export declare class UxWizardStepsComponentModule {
}
