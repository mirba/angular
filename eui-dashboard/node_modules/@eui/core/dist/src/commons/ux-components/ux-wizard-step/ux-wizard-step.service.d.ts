import { ActivatedRoute } from '@angular/router';
import { UxWizardStep } from './ux-wizard-step';
import { UxService } from '../../../core/app/ux.service';
export declare class UxWizardStepService {
    private uxService;
    activeStepIndex: number;
    steps: UxWizardStep[];
    route: ActivatedRoute;
    constructor(uxService: UxService);
    init(steps: UxWizardStep[], route: ActivatedRoute): void;
    navigationIncrement(increment: number): void;
    selectStep(step: UxWizardStep): void;
    private _navigateToStep(url);
}
