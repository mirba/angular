import { ElementRef, OnChanges, Renderer, SimpleChanges } from '@angular/core';
import { UxService } from '../../../core/app/ux.service';
export declare class UxDisableContentComponent implements OnChanges {
    private elRef;
    private renderer;
    private uxService;
    isDisabled: boolean;
    disabledText: string;
    private lastActiveElement;
    constructor(elRef: ElementRef, renderer: Renderer, uxService: UxService);
    ngOnChanges(changes: SimpleChanges): void;
    isAboutToBlock(changes: SimpleChanges): boolean;
    desactivateElement(activeElement: HTMLElement): void;
    reactivateElement(activeElement: HTMLElement): void;
    getActiveElement(): HTMLElement;
    onClick(event: any): void;
}
export declare class UxDisableContentComponentModule {
}
