import { QueryList, AfterContentInit, OnDestroy, OnChanges, SimpleChanges, EventEmitter, ElementRef } from '@angular/core';
import { Subscription } from 'rxjs/Subscription';
import { UxTabComponent } from './ux-tab.component';
import { UxTab } from './ux-tab';
import { UxService } from '../../../core/app/ux.service';
export declare class UxTabsComponent implements AfterContentInit, OnDestroy, OnChanges {
    private uxService;
    private el;
    styleClass: string;
    tabs: Array<UxTab>;
    isMainNavigation: boolean;
    isSubTabs: boolean;
    activeTabIndex: number;
    tabSelected: EventEmitter<UxTabComponent>;
    tabClosed: EventEmitter<UxTabComponent>;
    activeTabIndexChange: EventEmitter<{}>;
    subscription: Subscription;
    childrenTabs: QueryList<UxTabComponent>;
    constructor(uxService: UxService, el: ElementRef);
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    checkTabsWidthHandler(): void;
    ngOnChanges(changes: SimpleChanges): void;
    onSelectTab(tab: UxTab, index: number): void;
    onCloseTab(event: Event, tab: UxTab, index: number): void;
    private _selectTab(tab, index);
    private _getTab(index);
}
export declare class UxTabsRightContentTagDirective {
}
export declare class UxTabsComponentModule {
}
