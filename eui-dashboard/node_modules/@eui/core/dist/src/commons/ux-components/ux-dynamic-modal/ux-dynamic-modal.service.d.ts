import { DomPortalHost, Portal } from '@angular/cdk/portal';
import { UxDynamicComponentService } from '../../../core/app/ux-dynamic-component.service';
import { UxDynamicModalConfig } from './ux-dynamic-modal.config.model';
import { UxDynamicMessageBoxConfig } from '../ux-dynamic-message-box/ux-dynamic-message-box.config.model';
export declare class UxDynamicModalService {
    private uxDynamicComponentService;
    /**
     * Constructor
     */
    constructor(uxDynamicComponentService: UxDynamicComponentService);
    /**
     * Open a modal dynamically (Add it to the DOM)
     */
    openModal(config: UxDynamicMessageBoxConfig | UxDynamicModalConfig): any;
    /**
     * Close a modal created dynamically (Remove it from the DOM)
     */
    closeModal(portalHostRef: DomPortalHost, portalRef: Portal<any>): void;
}
