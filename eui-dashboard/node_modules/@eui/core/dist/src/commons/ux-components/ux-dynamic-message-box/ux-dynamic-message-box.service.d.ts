import { UxDynamicModalService } from '../ux-dynamic-modal/ux-dynamic-modal.service';
import { UxDynamicMessageBoxConfig } from '../ux-dynamic-message-box/ux-dynamic-message-box.config.model';
export declare class UxDynamicMessageBoxService {
    private uxDynamicModalService;
    private portalHost;
    private portal;
    constructor(uxDynamicModalService: UxDynamicModalService);
    /**
     * Open a message-box dynamically (Add it to the DOM)
     */
    openMessageBox(config: UxDynamicMessageBoxConfig): void;
    /**
     * Close a message-box created dynamically (Remove it from the DOM)
     */
    closeMessageBox(): void;
}
