import { Bounds } from './../interface/bounds';
export declare class Overflow implements Bounds {
    left: number;
    top: number;
    right: number;
    bottom: number;
    private constructor();
    static create(left: number, top: number, right: number, bottom: number): Overflow;
    equals(other: Overflow): boolean;
    toString(): string;
}
