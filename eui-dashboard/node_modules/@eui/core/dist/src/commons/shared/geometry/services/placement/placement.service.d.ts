import { Rectangle } from './../../model/rectangle';
import { PlacementOptions } from '../../interface/placement-options';
import { PlacementStrategy } from '../../interface/placement-strategy';
export declare class PlacementService {
    private placementStrategies;
    constructor(placementStrategies?: Array<PlacementStrategy>);
    static getEffectiveOptions(options?: PlacementOptions): PlacementOptions;
    static pickPlacementStrategy(placementStrategies: Array<PlacementStrategy>, placementId: string): PlacementStrategy;
    place(options?: PlacementOptions): Rectangle;
}
