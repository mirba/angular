import { Position } from './../interface/position';
export declare class Offset implements Position {
    x: number;
    y: number;
    private constructor();
    static create(x: number, y: number): Offset;
    equals(other: Offset): boolean;
    toString(): string;
}
