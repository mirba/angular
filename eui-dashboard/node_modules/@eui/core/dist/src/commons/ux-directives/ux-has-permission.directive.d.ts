import { ElementRef, OnInit, TemplateRef, ViewContainerRef } from '@angular/core';
import { UxPermissionService } from '../../core/permissions/ux-permission.service';
export declare class UxHasPermissionDirective implements OnInit {
    private element;
    private templateRef;
    private viewContainer;
    private permissionService;
    private permissions;
    private isHidden;
    constructor(element: ElementRef, templateRef: TemplateRef<any>, viewContainer: ViewContainerRef, permissionService: UxPermissionService);
    hasPermission: any;
    ngOnInit(): void;
    private updateView();
    private checkPermission();
}
export declare class UxHasPermissionDirectiveModule {
}
