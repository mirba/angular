'use strict';

const path = require('path');
const config = require('../config/project');
const injectHtml = require('./openid-connect/inject-html');

const { env, htmlFile, envConfigFolder } = config;

injectHtml(env, htmlFile, envConfigFolder);
