import { OnDestroy } from '@angular/core';
export declare class EclBaseComponent implements OnDestroy {
    constructor();
    ngOnDestroy(): void;
    renderTemplate(templateName: string, data?: any, blocks?: any): string;
}
