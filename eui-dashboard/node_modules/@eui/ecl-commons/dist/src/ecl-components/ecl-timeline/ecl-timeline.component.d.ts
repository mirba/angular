import { OnChanges, ViewContainerRef } from '@angular/core';
import { EclBaseComponent } from '../ecl-base-component';
export declare class EclTimelineComponent extends EclBaseComponent implements OnChanges {
    private vcRef;
    limit: any;
    buttonLabel: any;
    items: any;
    constructor(vcRef: ViewContainerRef);
    ngOnChanges(): void;
}
export declare class EclTimelineComponentModule {
}
