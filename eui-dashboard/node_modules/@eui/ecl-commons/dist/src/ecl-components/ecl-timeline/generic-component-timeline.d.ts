export declare const timelines: ({selector, buttonSelector, hiddenElementsSelector, classToRemove, context}?: {
    selector?: string;
    buttonSelector?: string;
    hiddenElementsSelector?: string;
    classToRemove?: string;
    context?: Document;
}) => void;
export default timelines;
