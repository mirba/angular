'use strict';

const findUp = require('find-up');
const fs = require('fs');

const configPath = findUp.sync('.euirc.json');
let config = {};

try {
  config = JSON.parse(fs.readFileSync(configPath));
} catch(e) {}

const argv = require('yargs')
  .config(config)
  .argv;

module.exports = argv;
