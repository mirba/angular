import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';
import { BuildingsOverviewComponent } from './components/buildings-overview.component';

@NgModule({
    imports: [
        RouterModule.forChild([
            { path: '', component: BuildingsOverviewComponent },
        ])
    ],
    exports: [
        RouterModule
    ]
})
export class BuildingsRoutingModule {
}
