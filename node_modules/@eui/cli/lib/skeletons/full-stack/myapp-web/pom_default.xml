<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
    <parent>
      <groupId>@app.group.id@.@app.name@</groupId>
      <version>0.0.1-SNAPSHOT</version>
      <artifactId>@app.name@</artifactId>
    </parent>

  <artifactId>@app.name@-web</artifactId>
  <packaging>war</packaging>
  <name>@app.name.display@ web module</name>

  <dependencies>
      <dependency>
          <groupId>eu.europa.ec.digit.uxatec.eui</groupId>
          <artifactId>eui-angular2-servlet</artifactId>
          <version>1.0.0</version>
      </dependency>

      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>@app.name@-web-rest</artifactId>
        <version>${project.version}</version>
      </dependency>

  </dependencies>


  <build>
    <finalName>@app.name.display@</finalName>
    <plugins>
        <plugin>
            <groupId>io.swagger</groupId>
            <artifactId>swagger-codegen-maven-plugin</artifactId>
            <version>2.2.1</version>
            <executions>
                <execution>
                    <phase>generate-sources</phase>
                    <goals>
                        <goal>generate</goal>
                    </goals>
                    <configuration>
                        <inputSpec>${basedir}/../@app.name@-web-rest/target/generated-sources/swagger/swagger.json</inputSpec>
                        <language>@app.group.id@.@app.name@.codegen.TypescriptClientCodegen</language>
                        <output>${basedir}/src/main/angular/src/app/shared/swagger</output>
                    </configuration>
                </execution>
            </executions>
            <dependencies>
                <dependency>
                    <groupId>${project.groupId}</groupId>
                    <artifactId>@app.name@-web-rest-swagger</artifactId>
                    <version>${project.version}</version>
                </dependency>
            </dependencies>
        </plugin>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-war-plugin</artifactId>
            <version>3.0.0</version>
            <configuration>
                <failOnMissingWebXml>false</failOnMissingWebXml>
                <webResources>
                    <resource>
                        <directory>src/main/angular/dist</directory>
                    </resource>
                </webResources>
            </configuration>
        </plugin>
    </plugins>
  </build>
</project>
