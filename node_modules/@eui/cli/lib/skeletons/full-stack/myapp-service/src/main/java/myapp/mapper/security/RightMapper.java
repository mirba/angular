package @app.group.id@.@app.name@.mapper.security;

import @app.group.id@.@app.name@.common.dto.security.RightDTO;
import @app.group.id@.@app.name@.entity.security.Right;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;
import org.mapstruct.Mappings;

import java.util.List;

@Mapper(componentModel = "spring", uses=RoleMapper.class)
public interface RightMapper {
    @Mappings(
            @Mapping(target = "roles", ignore = true)
    )
    RightDTO toDTO(Right entity);

    Right toEntity(RightDTO vo);

    List<RightDTO> toDTOList(List<Right> list);

    List<Right> toEntityList(List<RightDTO> list);
}

