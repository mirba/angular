package @app.group.id@.@app.name@.mapper.security;

import @app.group.id@.@app.name@.common.dto.security.UserDTO;
import @app.group.id@.@app.name@.entity.security.User;
import org.mapstruct.Mapper;

import java.util.List;


@Mapper(componentModel = "spring",uses=RoleMapper.class)
public interface UserMapper {
    UserDTO toDTO(User entity);

    User toEntity(UserDTO vo);

    List<UserDTO> toDTOList(List<User> list);

    List<User> toEntityList(List<UserDTO> list);
}