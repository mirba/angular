<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
    <parent>
      <groupId>@app.group.id@.@app.name@</groupId>
      <version>0.0.1-SNAPSHOT</version>        
      <artifactId>@app.name@</artifactId>
    </parent>
  
  <artifactId>@app.name@-web-swagger-ui</artifactId>
  <packaging>war</packaging>
  <name>@app.name.display@ web SwaggerUI module</name>


  <build>
    <finalName>@app.name.display@-swagger-ui</finalName>
    <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>1.7</version>
            <executions>
              <execution>
                <id>pre-install</id>
                <phase>generate-sources</phase>
                <configuration>
                  <target>
                    <copy file="${basedir}/../@app.name@-web-rest/target/generated-sources/swagger/swagger.json"
                          tofile="${basedir}/target/@app.name@-swagger-ui/swagger.json"/>
                  </target>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
          </plugin>     
    </plugins>
  </build>
</project>
