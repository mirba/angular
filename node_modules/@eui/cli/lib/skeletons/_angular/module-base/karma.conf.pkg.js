const karmaConfig = require('@eui/tools/karma/karma.conf.pkg');

module.exports = function (config) {
    config.set(
        karmaConfig.get(config, '@module.name@', {
            shimRootPath: 'src/packages/@module.name@/',
            files: [
                { pattern: 'node_modules/@eui/ux-commons/dist/**/*.js', included: false, watched: false },
                { pattern: 'node_modules/@eui/ux-core/dist/**/*.js', included: false, watched: false },
            ],
            proxies: {
                '/base/src/packages/@module.name@/src/lib/node_modules/': '/base/node_modules/',
            },
        })
    );
};
