package eu.europa.ec.digit.eui.repository;


import com.mysema.query.types.Predicate;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.querydsl.QueryDslPredicateExecutor;
import org.springframework.data.repository.CrudRepository;
import eu.europa.ec.digit.eui.entity.User;
import org.springframework.data.repository.query.Param;

import java.util.List;

/*
public interface UserRepository extends CrudRepository<User, Integer> {
    @Query("SELECT u FROM User u WHERE (LOWER(u.name) = LOWER(:name) OR :name = '') AND (LOWER(u.firstName) = LOWER(:firstName) OR :firstName = '')")
    List<User> find(@Param("name") String name, @Param("firstName") String firstName);
}
*/


public interface UserRepository extends CrudRepository<User, Integer>, QueryDslPredicateExecutor<User> {

}
